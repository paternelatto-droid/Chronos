<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChurchApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

<!--    <style name="Theme.ChurchApp" parent="Base.Theme.ChurchApp" />-->
    <style name="Theme.ChurchApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Couleurs principales -->
        <item name="mainColor">@color/colorPrimary</item>
        <item name="Background">@color/white</item>
        <item name="viewBackGround">@color/Grey</item>
        <item name="headViewColor">@color/Indigo</item>
        <item name="unChecked">@color/Grey</item>
        <item name="iconColor">@color/black</item>
        <item name="textColor">@color/black</item>
        <item name="notificationIconColor">@color/red</item>
    </style>

</resources>